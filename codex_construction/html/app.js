const app=document.querySelector('#app'), members=document.querySelector('#members'), badge=document.querySelector('#badge'), count=document.querySelector('#count'), toast=document.querySelector('#toast');let state={}, player='';
const post=(name,data={})=>fetch(`https://${GetParentResourceName()}/${name}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});
function render(s){state=s||{};count.textContent=`${s.players||0} / ${s.maxPlayers||4}`;badge.textContent=s.active?(s.started?'IN PROGRESS':'RECRUITING'):'AVAILABLE';members.innerHTML=(s.crew||[]).map(x=>`<span class="member"><i class="dot"></i>${x.name} <small>${x.completed||0} tasks</small></span>`).join('')||'<span class="muted">No crew formed yet. Create one to begin.</span>';document.querySelector('#create').style.display=s.active?'none':'';document.querySelector('#join').style.display=s.active&&!s.started&&!s.mine?'':'none';document.querySelector('#start').style.display=s.active&&s.leader===s.myId&&!s.started?'':'none';document.querySelector('#leave').style.display=s.active&&s.mine&&!s.started?'':'none'}
function notify(m){toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)}
document.querySelector('#close').onclick=()=>{app.classList.add('hidden');post('close')};document.querySelector('#create').onclick=()=>post('create');document.querySelector('#join').onclick=()=>post('join');document.querySelector('#leave').onclick=()=>post('leave');document.querySelector('#start').onclick=()=>post('start');window.addEventListener('message',e=>{let d=e.data;if(d.action==='open'){player=d.player||'';app.classList.remove('hidden');render(d.state);document.querySelector('#pay').textContent='€'+Number(d.payout||12000).toLocaleString('de-DE')}if(d.action==='state')render(d.state);if(d.action==='toast')notify(d.message);if(d.action==='ended')notify('Contract finished. Payment sent!')});document.addEventListener('keydown',e=>{if(e.key==='Escape'){app.classList.add('hidden');post('close')}});
